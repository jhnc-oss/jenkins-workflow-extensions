name: release

on:
  push:
    tags:
      - '*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    name: "release hpi"
    steps:
      - name: Build
        uses: offa/maven-action@9f5d347be226bdc684c2a44182fd19b2e8169442
        with:
          java-version: 17
          pmd: false
      - name: Release
        uses: softprops/action-gh-release@6da8fa9354ddfdc4aeace5fc48d7f679b5214090
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: target/*.hpi
